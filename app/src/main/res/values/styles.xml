<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AutoPanelApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- 冷启动系统 splash（Android 12+）：品牌背景色 + 应用图标，首帧就绪后过渡到 Theme.AutoPanelApp -->
    <style name="Theme.AutoPanelApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/window_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="postSplashScreenTheme">@style/Theme.AutoPanelApp</item>
    </style>
</resources>
